# 模板匹配方法说明

## 当前实际使用的方法

主程序实际运行时使用的是 `main.py` 中 `if __name__ == "__main__":` 内部定义的 `find_template()`。

核心算法是 OpenCV 模板匹配：

```python
cv2.matchTemplate(main_img, template, cv2.TM_CCOEFF_NORMED)
```

也就是归一化相关系数匹配。程序会在截图中滑动模板图，计算每个位置的相似度，再通过：

```python
cv2.minMaxLoc(result)
```

取最高相似度 `max_val` 和对应左上角坐标 `max_loc`。

## 普通匹配

普通匹配用于结算、存档等非严格识别。

流程：

1. 读取模板图。
2. 使用 `cv2.matchTemplate()` 匹配截图和模板。
3. 使用 `cv2.minMaxLoc()` 获取最高相似度。
4. 如果 `max_val >= threshold`，认为匹配成功。

默认阈值：

```python
threshold = 0.8
```

## 严格匹配

关卡识别使用严格匹配，当前调用参数：

```python
find_template(
    screen_img,
    trigger_template_path,
    threshold=0.8,
    strict=True,
    edge_threshold=0.3,
    focus_threshold=0.6
)
```

严格匹配包含三层判断。

### 1. 灰度模板匹配

截图和模板先转灰度：

```python
main_match_img = cv2.cvtColor(main_img, cv2.COLOR_BGR2GRAY)
template_match_img = cv2.cvtColor(template, cv2.COLOR_BGR2GRAY)
```

然后使用：

```python
cv2.matchTemplate(main_match_img, template_match_img, cv2.TM_CCOEFF_NORMED)
```

要求：

```python
max_val >= 0.8
```

### 2. 整体边缘校验

主匹配通过后，程序裁出候选区域：

```python
gray_patch = main_match_img[y:y + th, x:x + tw]
```

然后对候选区域和模板分别做 Canny 边缘检测：

```python
patch_edges = cv2.Canny(gray_patch, 50, 150)
template_edges = cv2.Canny(template_match_img, 50, 150)
```

再用模板匹配比较两张边缘图：

```python
edge_result = cv2.matchTemplate(patch_edges, template_edges, cv2.TM_CCOEFF_NORMED)
```

要求：

```python
edge_score >= 0.3
```

### 3. 左下角数字区域校验

为了重点匹配数字，当前不再使用左侧 `62%` 区域，而是把模板从横向和纵向平均切开，取左下角四分之一区域。

代码逻辑：

```python
focus_x2 = max(1, tw // 2)
focus_y1 = max(0, th // 2)
focus_patch = gray_patch[focus_y1:, :focus_x2]
focus_template = template_match_img[focus_y1:, :focus_x2]
```

也就是：

- 横向：取左半边；
- 纵向：取下半边；
- 最终区域：左下角四分之一。

再进行：

```python
cv2.matchTemplate(focus_patch, focus_template, cv2.TM_CCOEFF_NORMED)
```

要求：

```python
focus_score >= 0.6
```

## 当前阈值

| 用途 | 参数 | 当前值 |
|---|---:|---:|
| 普通匹配阈值 | `threshold` | `0.8` |
| 关卡严格匹配主阈值 | `threshold` | `0.8` |
| 严格模式整体边缘阈值 | `edge_threshold` | `0.3` |
| 严格模式左下角数字区域阈值 | `focus_threshold` | `0.6` |
| Canny 低阈值 | `50` | `50` |
| Canny 高阈值 | `150` | `150` |

## 测试脚本

测试脚本：

```text
test_template_match.py
```

示例：

```powershell
python test_template_match.py --image 1111.png --template img\40-2_.png --strict --threshold 0.8 --edge-threshold 0.3 --focus-threshold 0.6 --output result.png
```

脚本输出包括：

- 是否匹配成功；
- 失败原因；
- 候选区域左上角；
- 候选区域中心点；
- 主匹配分数；
- 整体边缘匹配分数；
- 左下角数字区域匹配分数。
